#include <stdio.h>			// 헤더파일 
/* 컴파일 시 운영체제가 메인함수 호출 */
// 1.함수선언, 2.정의, 3.호출
/* 출력 함수명(입력) */
int main(void) {						// 함수 선언 
	//int a = 65;
	//printf("%d %c Hello World!", a, a); // 함수 호출

	return 0;							// 자료형에 따른 출력 값 --> 호출한 곳(운영체제)에 돌려줌
}

